from src.Player import *

class PlayerAI:
    def __init__(self, player: Player) -> None:
        self.player = player
    
        def getTurnDecision(self, dealerUpcard: Card) -> str:
            if player.handIsSplittable() == True:
                return ""
            elif player.handIsSoft() == True:
                return ""
            else:
                return ""

            